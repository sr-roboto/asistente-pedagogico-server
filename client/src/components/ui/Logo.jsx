import React from 'react';

const Logo = () => (
    <div className="flex flex-col items-center justify-center py-4">
        <h1 className="text-3xl font-bold text-teal-800 tracking-tight font-serif">
            Buenos Aires
        </h1>
        <span className="text-2xl font-handwriting text-purple-600 -mt-2 transform -rotate-2">aprende</span>
    </div>
);

export default Logo;
